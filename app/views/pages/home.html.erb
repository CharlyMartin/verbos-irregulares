<div class="home" style="background-image: linear-gradient(-225deg, rgba(32, 164, 243, 0.2) 0%, rgba(252, 138, 0, 0.1) 50%), url(<%= asset_path 'home.jpg' %>);">
  <div class='text-container'>
    <h1 class="tagline"> <%= t('home.baseline') %> </h1>
    <h2> <%= link_to t('home.call-to-action'), method_path, class: 'home-button' %> </h2>
  </div>
</div>

<%= render "pages/shared/cta-pdf" %>

<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2 col-xs-12">

      <table>
        <thead>
          <tr>
            <th>Infinitivo</th>
            <th>Pasado Simple</th>
            <th>Pasado Participio</th>
            <th>Traducción</th>
          </tr>
        </thead>
        <tbody>
          <% @verbs.each do |verb| %>
            <tr>
              <td> <%= verb.infinitive %> </td>
              <td> <%= verb.simple_past %> </td>
              <td> <%= verb.past_participle %> </td>
              <td> <%= verb.translation %> </td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
</div>
